try
set ret to do shell script "PGHYPERIC_INSTALLDIR/agent-PGHYPERIC_VERSION_STR/bin/hq-agent.sh stop > /tmp/pgmsg.txt 2>&1" with administrator privileges
end try
set output to do shell script "cat /tmp/pgmsg.txt" with administrator privileges
set ret to do shell script "rm /tmp/pgmsg.txt" with administrator privileges
display dialog output buttons {"OK"} with icon 1 default button 1
